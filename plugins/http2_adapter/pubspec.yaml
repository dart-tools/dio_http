name: dio_http2_adapter
description: A Dio HttpClientAdapter which support Http/2.0(Support connection reuse, header compression, server pushing is not supported yet.)
version: 2.0.0
homepage: https://github.com/dart-tools/dio_http/tree/master/plugins/http2_adapter

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  http2: ^2.0.0
  dio_http: ^5.0.3
#  dio:
#    path: ../../dio

dev_dependencies:
  test: ^1.17.11
  pedantic: ^1.10.0

